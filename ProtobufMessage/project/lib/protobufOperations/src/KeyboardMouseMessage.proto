syntax = "proto3";
package KeyboardMouse;

message ButtonsCoords
{
    repeated bool buttonPressed = 1 [ packed = true ]; // repeated => buttonPressed is array
    int64 xCoord = 2;
    int64 yCoord = 3;
    //repeated bool mouseClick = 4 [ packed = true ];
}